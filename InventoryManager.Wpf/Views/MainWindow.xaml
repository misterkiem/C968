<Window x:Class="InventoryManager.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventoryManager.Wpf.Views"
        xmlns:vm="clr-namespace:InventoryManager.Wpf.Vms"
        mc:Ignorable="d"
        Title="Main Screen"
        MinHeight="300"
        MinWidth="800"
        Height="450"
        Width="800"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowVm}"
        Background="#F0F0F0">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label FontSize="20"
               FontWeight="Bold">
            Inventory Management System
        </Label>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <local:MainWindowCard DataContext="{Binding PartsCardVm}"/>
            <local:MainWindowCard Grid.Column="1" DataContext="{Binding ProductsCardVm}" />
        </Grid>
    </Grid>
</Window>
